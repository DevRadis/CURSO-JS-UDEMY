// String, Number, Undefined, Null e Symbol que será aprendido em outra aula.
const nome = 'Paulo' //String
const nome1 = "Paulo" //String
const nome2 = `Paulo` //String
const num1 = 10 // Number
const num2 = 10.52 //Number
let nomeAluno // undefined -> não aponta pra local nenhum na memória
const sobrenomeAluno = null // Nulo -> não aponta pra local nenhum na memória
const aprovado = true // boolean = false, true (lógico)

console.log(typeof nomeAluno)